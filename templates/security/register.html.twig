{% extends "base_front.html.twig" %}

{% block title %} Inscription d'un utilisateur {{ parent() }}{% endblock %}

{% block body %}
    <div class="container bootstrap snippet">

        <div class="container mt-4">
            <h4>Inscription d'un utilisateur</h4>
            {{ form_start(form) }}
            <div>
                <img src="{{ asset('img/no-image.png') }}" class="avatar" alt="avatar">
{#                <h6>Mettez un photo...</h6>#}
{#                <input type="file" class="text-center center-block file-upload">#}
                <div class="row">
                    <div class="col-md-5">{{ form_row(form.picture) }}</div>
                </div>
            </div></hr><br>

            <div class="row">
                <div class="col-md-4">{{ form_row(form.nom) }}</div>
                <div class="col-md-4">{{ form_row(form.prenom) }} </div>
            </div>

            <div class="row">
                <div class="col-md-3">{{ form_row(form.username) }}</div>
                <div class="col-md-3">{{ form_row(form.telephone) }}</div>
            </div>

            <div class="row">
                <div class="col-md-4">{{ form_row(form.email) }}</div>
                <div class="col-md-4">{{ form_row(form.adresse) }}</div>
            </div>

            <div class="row">
                <div class="col-md-5">{{ form_row(form.fonction) }}</div>
                <div class="col-md-3">{{ form_row(form.departement) }}</div>
            </div>

            <div class="row">
                <div class="col-md-4">{{ form_row(form.password) }}</div>
                <div class="col-md-4">{{ form_row(form.confirm_password) }}</div>
            </div>
            <button type="submit" class="btn btn-primary">Enregistrer</button>
            {{ form_end(form) }}
        </div>
    </div>

{% endblock %}