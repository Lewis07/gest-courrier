{% extends "base_admin.html.twig" %}
{% block title %}{{ parent() }} Détail du courrier{% endblock %}

{% block body %}
    {% include 'partials/header.html.twig' %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-12">
                <div class="page-title-box">
                    <div class="row">
                        <div class="col">
                            <h4 class="page-title">Courriers</h4>
                            <ol class="breadcrumb pt-3 d-flex align-items-center">
                                <li class="breadcrumb-item">
                                    <a href="javascript:void(0);">Gestion de Courrier</a>
                                </li>
                                <li class="breadcrumb-item">Courrier</li>
                                <li class="breadcrumb-item active">Détail</li>
                            </ol>
                        </div><!--end col-->
                    </div><!--end row-->
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-4">
                <div class="mb-2">
                    <span class="font-weight-bold">Envoyeur :</span>
                    <span>{{ courrier is defined ? courrier.sender.email : "" }}</span>
                </div>
                <div class="mb-2">
                    <span class="font-weight-bold">Recepteur :</span>
                    <span>{{ courrier is defined ? courrier.recipient.email : "" }}</span>
                </div>
                <div class="mb-2">
                    <span class="font-weight-bold">Objet :</span>
                    <span>{{ courrier is defined ? courrier.objetCourrier : "" }}</span>
                </div>
                <div class="mb-2">
                    <span class="font-weight-bold">Date d'envoi :</span>
                    <span>
                        {{ courrier is defined ? courrier.dateEnvoie|date('d/m/Y') : "" }} à
                        {{ courrier is defined ? courrier.dateEnvoie|date('H:i') : "" }}
                    </span>
                </div>
                <div class="mb-2">
                    <span class="font-weight-bold">Type du courrier :</span>
                    <span>{{ courrier.typeCourrier is defined ? courrier.typeCourrier.libelleTypeCourrier }}</span>
                </div>
                {% if courrier.fichier is defined %}
                    <div class="mb-2 {{ courrier.fichier is null ? 'd-none' : '' }}">
                        <span class="font-weight-bold">
                            <a href="{{ asset('uploads/courriers/' ~ courrier.fichier ) }}">Voir le courrier</a>
                        </span>
                    </div>
                {% endif %}
                <div class="mt-4">
                    <span class="font-weight-bold">Message :</span>
                    <span>{{ courrier is defined ? courrier.message|raw }}</span>
                </div>
            </div>
        </div>
    </div>
{% endblock %}